import { Request, Response } from 'express'
const asyncHandler = require('express-async-handler')

const handleAccountSetup = asyncHandler(async (req: Request, res: Response) => {

})

export default handleAccountSetup